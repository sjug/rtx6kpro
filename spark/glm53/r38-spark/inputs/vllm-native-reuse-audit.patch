diff --git a/CMakeLists.txt b/CMakeLists.txt
index 1c3b2e49607cc386a407b0a80fdc0c4dea97469b..93f0c054f565aaad99d8298c4dcc2df5e5deb82e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -31,6 +31,8 @@ endif()
 
 # CUDA by default, can be overridden by using -DVLLM_TARGET_DEVICE=... (used by setup.py)
 set(VLLM_TARGET_DEVICE "cuda" CACHE STRING "Target device backend for vLLM")
+option(VLLM_BUILD_CUTLASS_SCALED_MM_C2X
+  "Build CUTLASS 2.x scaled-mm kernels for pre-SM90 NVIDIA GPUs" ON)
 message(STATUS "Build type: ${CMAKE_BUILD_TYPE}")
 message(STATUS "Target device: ${VLLM_TARGET_DEVICE}")
 
@@ -408,18 +410,29 @@ if(VLLM_GPU_LANG STREQUAL "CUDA" OR VLLM_GPU_LANG STREQUAL "HIP")
     if(TORCH_STABLE_STRING_CONTENTS MATCHES "auto strptr = new std::string")
       set(TORCH_SHADOW_INCLUDE_DIR
         "${CMAKE_CURRENT_BINARY_DIR}/torch_patched_headers")
+      set(TORCH_SHADOW_STAGING_DIR
+        "${CMAKE_CURRENT_BINARY_DIR}/torch_patched_headers_staging")
 
       file(MAKE_DIRECTORY "${TORCH_SHADOW_INCLUDE_DIR}/torch/csrc/stable")
+      file(REMOVE_RECURSE "${TORCH_SHADOW_STAGING_DIR}")
+      file(MAKE_DIRECTORY "${TORCH_SHADOW_STAGING_DIR}/torch/csrc/stable")
       file(COPY_FILE
         "${TORCH_INCLUDE_DIR}/${_stable_hdr_rel}"
-        "${TORCH_SHADOW_INCLUDE_DIR}/${_stable_hdr_rel}")
+        "${TORCH_SHADOW_STAGING_DIR}/${_stable_hdr_rel}")
 
       execute_process(
         COMMAND patch --batch --forward -p1
           "--input=${CMAKE_CURRENT_LIST_DIR}/cmake/patches/pytorch_stable_string.patch"
-        WORKING_DIRECTORY "${TORCH_SHADOW_INCLUDE_DIR}"
+        WORKING_DIRECTORY "${TORCH_SHADOW_STAGING_DIR}"
         COMMAND_ERROR_IS_FATAL ANY)
 
+      # Header timestamps participate in Ninja dependency checks. Preserve the
+      # destination timestamp when the patched content has not changed.
+      file(COPY_FILE
+        "${TORCH_SHADOW_STAGING_DIR}/${_stable_hdr_rel}"
+        "${TORCH_SHADOW_INCLUDE_DIR}/${_stable_hdr_rel}"
+        ONLY_IF_DIFFERENT)
+
       include_directories(BEFORE "${TORCH_SHADOW_INCLUDE_DIR}")
     endif()
   endif()
@@ -896,8 +909,14 @@ if(VLLM_GPU_LANG STREQUAL "CUDA" OR VLLM_GPU_LANG STREQUAL "HIP")
   # For the cutlass_scaled_mm kernels we want to build the c2x (CUTLASS 2.x)
   # kernels for the remaining archs that are not already built for 3x.
   # (Build 8.9 for FP8)
-  cuda_archs_loose_intersection(SCALED_MM_2X_ARCHS
-    "7.5;8.0;8.7;8.9+PTX" "${CUDA_ARCHS}")
+  if(VLLM_BUILD_CUTLASS_SCALED_MM_C2X)
+    cuda_archs_loose_intersection(SCALED_MM_2X_ARCHS
+      "7.5;8.0;8.7;8.9+PTX" "${CUDA_ARCHS}")
+  else()
+    set(SCALED_MM_2X_ARCHS)
+    message(STATUS "Not building scaled_mm_c2x because "
+                   "VLLM_BUILD_CUTLASS_SCALED_MM_C2X is OFF")
+  endif()
   # subtract out the archs that are already built for 3x
   list(REMOVE_ITEM SCALED_MM_2X_ARCHS ${SCALED_MM_3X_ARCHS})
   if (SCALED_MM_2X_ARCHS)
diff --git a/setup.py b/setup.py
index ae64a13daa0f1facc255afcdb4ffcad264776b98..ce6681d21b58433f37679e0e1655dc83e097aa56 100644
--- a/setup.py
+++ b/setup.py
@@ -20,6 +20,7 @@ from setuptools import Extension, setup
 from setuptools.command.build_ext import build_ext
 from setuptools_rust.build import build_rust
 from setuptools_scm import get_version
+from setuptools_scm.git import DEFAULT_DESCRIBE
 from torch.utils.cpp_extension import CUDA_HOME, ROCM_HOME
 
 
@@ -1261,7 +1262,11 @@ def get_vllm_version() -> str:
         os.environ["SETUPTOOLS_SCM_PRETEND_VERSION"] = env_version
         return get_version(write_to="vllm/_version.py")
 
-    version = get_version(write_to="vllm/_version.py")
+    # Source-addressed wheel releases are artifact identities, not package versions.
+    version = get_version(
+        write_to="vllm/_version.py",
+        git_describe_command=[*DEFAULT_DESCRIBE, "--exclude", "vllm-jovian-cu134-*"],
+    )
     sep = "+" if "+" not in version else "."  # dev versions might contain +
 
     if _no_device():
diff --git a/tools/build_rust.py b/tools/build_rust.py
index a000ec8169fd7acafac3f99a4350998f350fa32a..ce60248b38036cfb59140d87e26ce0fdb131a869 100644
--- a/tools/build_rust.py
+++ b/tools/build_rust.py
@@ -12,6 +12,7 @@ from pathlib import Path
 from setuptools import setup
 from setuptools_rust import Binding, RustExtension
 from setuptools_scm import get_version
+from setuptools_scm.git import DEFAULT_DESCRIBE
 
 ROOT_DIR = Path(__file__).resolve().parents[1]
 VLLM_RS_BUILD_VERSION = "VLLM_RS_BUILD_VERSION"
@@ -22,7 +23,14 @@ def prepare_build_environment() -> str | None:
     version = os.getenv(VLLM_RS_BUILD_VERSION) or None
     if version is None:
         try:
-            version = get_version(root=ROOT_DIR)
+            version = get_version(
+                root=ROOT_DIR,
+                git_describe_command=[
+                    *DEFAULT_DESCRIBE,
+                    "--exclude",
+                    "vllm-jovian-cu134-*",
+                ],
+            )
         except LookupError:
             return None
 
